package acsse.csc3a.miniProject;

/**
 * Loads image files from disk and converts them into a usable format
 * for processing (e.g., BufferedImage).
 */
public class ImageLoader {
    // Load image by file path
}

