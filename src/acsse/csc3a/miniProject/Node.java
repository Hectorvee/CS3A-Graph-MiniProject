package acsse.csc3a.miniProject;

/**
 * Represents a node in the graph, which could correspond to a pixel,
 * superpixel, or image feature point.
 */
public class Node {
    // Node id, coordinates, and optional feature vector.
}

